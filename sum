#include<stdio.h>
int add();
void main(){
    int total;
        total= add();
       printf("The addition of two numbers:%d \n",total);
}
int add()
{
    int num1,num2;
    printf("Enter two numbers:");
    scanf("%d %d",&num1,&num2);
    return (num1+num2);
}
